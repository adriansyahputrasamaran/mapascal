# Contoh file .env untuk konfigurasi aplikasi MAPASCAL
# Ganti nilai-nilai berikut dengan nilai yang sesuai untuk lingkungan Anda.

# Kunci rahasia aplikasi Flask. HARUS KUAT dan acak, minimal 32 karakter.
# Anda bisa membuat kunci acak dengan: python -c 'import os; print(os.urandom(24).hex())'
SECRET_KEY='your_super_secret_key_here_at_least_32_chars_long'

# URL koneksi database SQLAlchemy.
# Contoh untuk MySQL: mysql+pymysql://user:password@host:port/database_name
DATABASE_URL='mysql+pymysql://root:@localhost/mapascal_db'

# Set FLASK_DEBUG ke 'true' untuk mode debug (HANYA UNTUK PENGEMBANGAN!)
# FLASK_DEBUG='false'
